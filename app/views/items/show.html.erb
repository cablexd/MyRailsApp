<h1><%= @item.name %></h1>

<p><%= @item.description || "No description." %></p>

<%= render "items/item_status", item: @item %>

<%= link_to "Back", items_path %>
<%= link_to "Edit", edit_item_path %>
<%= button_to "Delete", @item, method: :delete %>